# Only track the largest files with LFS to stay within GitHub limits and ensure Vercel compatibility
vamshieee/VIDEO-2025-10-13-16-42-28.mp4 filter=lfs diff=lfs merge=lfs -text

# All other files should be tracked normally to avoid Vercel LFS issues
# Remove any broad patterns that track small files with LFS
# This will significantly reduce LFS usage and improve Vercel deployment reliability
